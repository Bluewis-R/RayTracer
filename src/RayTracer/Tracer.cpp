#include "Tracer.h"

shared<glm::vec3> Tracer::TraceRay(Ray _ray)
{
  return shared<glm::vec3>();
}

glm::vec3 Tracer::ClosetPoint(shared<Ray> _ray)
{
  _ray->GetOrigin


  //X = a + ((P-a)DOT n)n
  // a => ray origin
  // p => position of sphere
  // n => is the ray direction


  return glm::vec3();
}
